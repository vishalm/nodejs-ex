name: CI
on: [push]

jobs:
  build:
    env:
      HTTPS_PROXY: "http://7e298c0a.ngrok.io/"
      HTTP_PROXY: "http://7e298c0a.ngrok.io/"

    runs-on: self-hosted

    steps:
    - name: TestLucaAction
      uses: lstocchi/openshift-actions@1.6.3
      with:
        version: '3.11.96'
        openshift_server_url: ${{ secrets.OPENSHIFT_SERVER_URL }}
        parameters: '{"apitoken": "${{ secrets.API_TOKEN }}", "acceptUntrustedCerts": "true"}'
        cmd: |
          'version'
          'get pods'
      

